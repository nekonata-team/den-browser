# MVP uses one shared web profile

Den Browser's MVP uses one shared persistent web profile for all desks, boards, and sheets. Boards are separate task contexts, but they do not imply separate login profiles. Profile separation can come later as a separate Den or window-level concept, similar to using a different browser profile in a conventional browser.
