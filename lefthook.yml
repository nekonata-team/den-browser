pre-commit:
  commands:
    format:
      glob: "*.swift"
      run: just format-staged {staged_files}
      stage_fixed: true
    lint:
      run: just lint

pre-push:
  commands:
    check:
      run: just check
